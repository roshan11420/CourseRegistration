spring.application.name=CourseRegistration

# PostgreSQL (configured by user)
spring.datasource.url=jdbc:postgresql://localhost:5432/finguard
spring.datasource.username=postgres
spring.datasource.password=977675@1122
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO

server.port=8080

# Embedded H2 (disabled). Uncomment to use H2 instead of Postgres.
# spring.datasource.url=jdbc:h2:mem:course_reg;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
